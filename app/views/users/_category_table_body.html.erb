<tr>
  <td><%= link_to category.name.underscore.humanize, category_user_path(user, category) %></td>
  <td><%= category.posts.question.size %></td>
  <td><%= category.posts.answered_by_user(user).size %></td>
</tr>
