<div class="col-md-2 col-sm-2 col-xs-1 qa-q-tabs-title">
  <h2>All Questions</h2>
</div>
<div class="nav-tab-bound">
  <ul class="nav nav-tabs qa-nav-tabs-right">
    <li class="menu-second-top-nav"><%= link_to "Unanswered", unanswered_path, title: "Switch to unanswered tabs" %></li>
    <li class="menu-second-top-nav <%= @tab == "most_answers" ? :active : nil %>">
      <%= link_to "Most answers", questions_path(tab: :most_answers), title: "Questions with the most answers" %>
    </li>
    <li class="menu-second-top-nav <%= @tab == "newest" ? :active : nil %>">
      <%= link_to "Newest", questions_path(tab: :newest), title: "The most recently asked questions" %>
    </li>
    <li class="menu-second-top-nav <%= @tab == "votest" ? :active : nil %>">
      <%= link_to "Votes", questions_path(tab: :votest), title: "Questions with the most votes" %>
    </li>
  </ul>
</div>

<div class="tab-content">
  <div class="qa-main">
    <div class="qa-part-q-list">
      <div class="qa-q-list">
        <%= render partial: "question", collection: @questions %>
        <%= paginate @questions, params: {tab: @tab} %>
      </div>
    </div>
  </div>
  <%= render partial: "sidepanel", locals: {tags: @tags} %>
</div>
